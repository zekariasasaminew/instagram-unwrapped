export function PrivacyNotice() {
  return (
    <p className="subtitle">
      Everything runs in your browser — your zip is never uploaded anywhere.
      Nothing is stored after you close this tab.
    </p>
  );
}
